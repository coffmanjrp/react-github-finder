import { Alert } from '../layout';
import { Search, Users } from '../users';

const Home = () => {
  return (
    <>
      <Alert />
      <Search />
      <Users />
    </>
  );
};

export default Home;
